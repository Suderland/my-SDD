# End-to-End Testing

Cover critical journeys through public system entry points. Keep the suite intentionally smaller than lower-level suites. Test observable outcomes, isolate state, use production-like builds/configuration when practical, and treat flakiness as a defect. For UI tests prefer user-facing locators and built-in waiting over arbitrary sleeps.
