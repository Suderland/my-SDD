# Contract Testing

Use for independently evolving consumers/providers across HTTP, RPC or messages. Verify consumer-relevant compatibility, not the provider's entire behavior. Keep contracts no stricter than compatibility requires. If no independently consumed boundary exists, contract testing can be inapplicable.
