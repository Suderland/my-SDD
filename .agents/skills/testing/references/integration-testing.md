# Integration Testing

Use where risk arises from real component or infrastructure interaction: persistence mappings/migrations, transactions, serialization, filesystem, cache, queue, middleware/configuration, or controlled external adapters. Prefer real ephemeral/local test infrastructure when deterministic and practical. Isolate data and ordering.
