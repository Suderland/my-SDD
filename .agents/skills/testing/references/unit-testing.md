# Unit Testing

Use for deterministic project-owned behavior. Test public behavior and invariants, not private structure. Favor small hermetic tests, boundary/error cases, parameterized cases for rule matrices, and domain state transitions. Avoid trivial getters, framework behavior, shared mutable fixtures, and excessive internal mocks.
