# Property-Based Testing

Use where general invariants are stronger than finite examples: parsers/serializers, round trips, normalization, algorithms, numeric/domain invariants and combinatorial inputs. Express meaningful properties such as round-trip, idempotence, bounds and invariant preservation. Keep generators inside valid domain constraints unless testing invalid input explicitly.
